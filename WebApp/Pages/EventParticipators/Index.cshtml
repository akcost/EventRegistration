@page
@model WebApp.Pages.EventParticipators.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EventParticipator[0].EventInfo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventParticipator[0].Participator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EventParticipator) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EventInfo.EventName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Participator.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
